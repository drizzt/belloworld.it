# The base URL of the site; the only required configuration variable.
base_url = "https://belloworld.it"

# The site title and description; used in feeds by default.
title = "Bello World"
description = "Bello World Projects"

# The default language; used in feeds.
default_language = "en"

# When set to "true", the Sass files in the `sass` directory in the site root are compiled.
# Sass files in theme directories are always compiled.
compile_sass = true

# When set to "true", the generated HTML files are minified.
minify_html = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `theme` variable to a theme supported by Zola
smart_punctuation = true

[markdown.highlighting]
theme = "github-dark"

[extra]
# User settings
username = "Timothy Mario Redaelli"
# Sits under the site title in the header. Empty today, kept because the header
# reads it.
user_title = ""
email = "&#098;&#101;&#108;&#108;&#111;&#064;&#098;&#101;&#108;&#108;&#111;&#119;&#111;&#114;&#108;&#100;&#046;&#105;&#116;"
